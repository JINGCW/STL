local SDL = require("SDL")
--local sdl_module=require("SDL")

local font = require("ttf")

print(SDL)
print(ttf)
print(font)
--local ret, err = SDL.init { SDL.flags.Video }
--if not ret then
--    error(err)
--end
